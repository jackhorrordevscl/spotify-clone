# Dockerfile.dev — frontend desarrollo con Vite HMR
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# --host expone el servidor en 0.0.0.0 para que Docker lo reenvíe al host
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]